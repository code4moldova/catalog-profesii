import React from 'react';
import { Container } from '../grid/container.component';

const Input = () => {
  return (
    <div>
      <input type="text" placeholder="Căutare profesie" />
    </div>
  );
};

export default Input;
